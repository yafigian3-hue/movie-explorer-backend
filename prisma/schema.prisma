datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Favorite {
  id        Int    @id
  title     String
  image     String
  rating    Float
  year      Int
}