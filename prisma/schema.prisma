generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Favorite {
  id        Int    @id
  title     String
  image     String
  rating    Float
  year      Int
}